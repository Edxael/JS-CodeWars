This repo have the answers for the site "www.codewars.com", I do one a day to practice JavaScript and put the answer on the repository to practice Git. 


The following are some notes for me:

// initialize repo
git init


git add *

git commit -m "Mesage here"

git remote add origin https://github.com/Edxael/JS-CodeWars.git

git push origin master




   *** the other notes ***

good place with videos to learn Git & Github:  dataschol.io on page "3" Git & Github Videos

How to clone a repository:
- create the repository online
- create and access work folder on PC
-   $ git clone http.....            // to clone the directory form onoline to the computer
- cd directory
- create or paste files

- check status
-  $ git add * (enter)                // to add all the files 

-  $ git remote -v                   // to chekc the remote locations
-  $ git commit -m "mesage here"     // to commit the changes
-  $ git log                         // to check the histoty
-  $ git push origen master          // to push the changed files. 

